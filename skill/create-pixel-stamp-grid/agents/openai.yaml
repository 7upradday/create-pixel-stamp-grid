interface:
  display_name: "Pixel Stamp Grid"
  short_description: "Make two color-faithful pixel-stamp posters"
  default_prompt: "Use $create-pixel-stamp-grid to transform these four photos into two pixel-stamp posters while preserving the original colors of each generated tile."
